<?xml version="1.0" encoding="utf-8"?>
<!--
  Permits plain HTTP (cleartext) only to the dev backend hosts used while
  building against a local FastAPI server. 10.0.2.2 is the Android emulator's
  alias for the host machine's localhost. Add your machine's LAN IP here too
  if testing against a physical device. Remove this once the backend is
  served over HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
